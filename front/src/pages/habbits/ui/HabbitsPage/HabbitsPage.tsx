import { Sidebar } from '@/widgets';

import classes from './Habbits.module.css';

export const HabbitsPage = () => {
  return (
    <div className={classes.habbits}>
      <Sidebar />
    </div>
  );
};
